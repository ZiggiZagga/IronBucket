server:
  port: 0

management:
  server:
    port: 0
  health:
    vault:
      enabled: false

spring:
  cloud:
    discovery:
      enabled: false
    vault:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

ironbucket:
  security:
    presigned:
      enabled: true
      secret: test-presigned-secret
      nonce-ttl: PT5M
